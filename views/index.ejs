<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Styled Form</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #FDFCDC; /* Background color from palette */
           
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center; /* Center text for introductory paragraphs */
        }

        body > p {
            font-size: 18px;
            color: #0081A7; /* Dark teal color */
            margin: 0 0 20px;
        }

        form {
            background-color: #00AFB9; /* Light teal color */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 300px;
            animation: fadeIn 1s ease-in;
            margin: auto;
        }

        label {
            color: #0081A7; /* Dark teal color */
            font-weight: 700;
            display: block;
            margin-bottom: 10px;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 2px solid #F07167; /* Coral color */
            border-radius: 4px;
            margin-bottom: 15px;
            box-sizing: border-box;
            transition: border-color 0.3s;
        }

        input[type="text"]:focus {
            border-color: #FED9B7; /* Peach color */
            outline: none;
        }

        button {
            background-color: #F07167; /* Coral color */
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #FDFCDC; /* Light background color */
            color: #0081A7; /* Dark teal color */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 600px) {
            form {
                width: 90%; /* Responsive width for smaller screens */
            }
        }
    </style>
</head>
<body>
    <h1>Welcome to the Weather Information</h1>
    <h1>Enter the Name of the city whose weather you want to check!</h1>
    <form action="/p" method="POST">
        <label for="city">ENTER THE CITY:</label>
        <input type="text" name="city" id="city">
        <button type="submit">Press</button>
    </form>
</body>
</html>


