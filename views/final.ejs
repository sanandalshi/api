<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Information</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #FDFCDC; /* Background color from palette */
            color: #0081A7; /* Dark teal color for text */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .weather-info {
            background-color: #00AFB9; /* Light teal color */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 300px;
            animation: fadeIn 1s ease-in;
        }

        .weather-info p {
            margin: 10px 0;
        }

        .weather-info img {
            width: 50px; /* Adjust size as needed */
            height: 50px;
        }

        .weather-info .temperature {
            font-size: 24px;
            font-weight: 700;
            color: #F07167; /* Coral color */
        }

        .weather-info .season {
            font-weight: 700;
            color: #0076A3; /* Darker teal color for more vibrancy */
        }

        .weather-info .description {
            font-style: italic;
            color: #FF6F61; /* Brighter coral color */
        }

        .weather-info .pressure {
            font-weight: 400;
            color: #006D77; /* More vibrant dark teal color */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="weather-info">
        <p>Weather of the city <%= city %></p>
        <p><%= city %> <img src="<%= icon %>" alt="Weather Icon"/></p>
        <p class="temperature"><%= temp %>&deg;C</p>
        <p class="season">The season is <%= main %></p>
        <p class="description"><%= discription %></p>
        <p class="pressure">Pressure: <%= pres %> hPa</p>
    </div>
</body>
</html>

